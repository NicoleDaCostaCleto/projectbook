{% extends 'base.html.twig' %}

{% block title %}
    {{ book.title }}
{% endblock %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="book">
                    <h2>{{ book.title }}</h2>
                    <p>Nombre de pages : {{ book.nbPages }}</p>
                    <p>Publié le : {{ book.publishedAt | date('d-m-y') }}</p>
                    {% if book.author is not null %}
                        auteur :<a href="{{ path('admin_show_author', {'id' : book.author.id}) }}">{{ book.author.firstName }} {{ book.author.lastName }}</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}