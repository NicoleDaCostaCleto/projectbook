{% extends 'admin/base.html.twig' %}

{% block main %}
    <section>
        {% for book in books %}
                <h1>{{ book.title }}</h1>
                <a href="{{ path('admin_show_book', {'id': book.id }) }}" class="btn">Voir le livre</a>
        {% endfor %}
    </section>
{% endblock %}